services:
  db:
    image: "${IMAGE}"
    environment:
      POSTGRES_USER: "${PGUSER}"
      POSTGRES_PASSWORD: "${PGPASSWORD}"
      POSTGRES_DB: "${PGDATABASE}"
    ports:
      - "${PG_PORT_MAPPING}"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    ports:
      - "8080:8080"
    volumes:
      - appdata:/app
      - ./wait-for.sh:/app/wait-for.sh
      - ./app.env:/app/app.env
      - ./.env:/app/.env
    environment:
      - DATABASE_URL=postgresql://${PGUSER}:${PGPASSWORD}@db:5432/${PGDATABASE}?sslmode=disable
    entrypoint: ["/app/wait-for.sh", "db:5432", "--timeout=400", "--", "/app/main"]

volumes:
  pgdata:
  appdata:

